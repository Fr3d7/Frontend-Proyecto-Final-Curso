pipeline {
  agent any

  options {
    timestamps()
    ansiColor('xterm')
    disableConcurrentBuilds()
    skipDefaultCheckout(true)   // evita checkout implícito
  }

  environment {
    CI = ''                         // CRA: no tratar warnings como errores
    NODE_ENV = 'production'
    NODE_OPTIONS = "--max_old_space_size=4096"
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
        script {
          @NonCPS
          def detectEnv = {
            def n = (env.JOB_NAME ?: '').toLowerCase()
            if (n.contains('prod')) return 'PROD'
            if (n.contains('qa'))   return 'QA'
            return (env.BRANCH_NAME ?: 'DEV')
          }
          env.EFFECTIVE_BRANCH = detectEnv()
          echo "📥 Rama/entorno efectivo: ${env.EFFECTIVE_BRANCH}"
        }
      }
    }

    stage('Install dependencies') {
      steps {
        echo "📦 npm ci (o npm install)"
        bat 'npm ci || npm install'
      }
    }

    stage('Run tests (coverage)') {
      steps {
        echo "🧪 npm test -- --coverage --watchAll=false --ci --passWithNoTests"
        bat 'npm test -- --coverage --watchAll=false --ci --passWithNoTests'
      }
      post {
        unsuccessful { echo "❌ Fallaron las pruebas unitarias" }
        success      { echo "✅ Pruebas unitarias OK" }
      }
    }

    stage('Build app') {
      steps {
        echo "🏗  npm run build"
        bat '''
          set CI=
          npm run build
        '''
      }
    }

    stage('SonarQube Analysis') {
      when { expression { env.EFFECTIVE_BRANCH in ['DEV','QA','PROD'] } }
      steps {
        echo "🔍 Enviando análisis a SonarQube…"
        withSonarQubeEnv('sonar-local') { // nombre EXACTO del servidor en Manage Jenkins > SonarQube servers
          withCredentials([string(credentialsId: 'sonarqube-token', variable: 'SONAR_TOKEN')]) {
            script {
              def scannerHome = tool 'sonar-scanner-win' // nombre EXACTO del Tool en Global Tool Configuration
              def pKey  = "frontend-proyecto-final-${env.EFFECTIVE_BRANCH}"
              // ⚠️ SIN paréntesis ni espacios sin comillas:
              def pName = "frontend-proyecto-final-${env.EFFECTIVE_BRANCH}"

              bat """
                "${scannerHome}\\bin\\sonar-scanner.bat" ^
                  -D"sonar.projectKey=${pKey}" ^
                  -D"sonar.projectName=${pName}" ^
                  -D"sonar.sources=src" ^
                  -D"sonar.sourceEncoding=UTF-8" ^
                  -D"sonar.javascript.lcov.reportPaths=coverage\\lcov.info" ^
                  -D"sonar.token=%SONAR_TOKEN%"
              """
            }
          }
        }
      }
    }

    stage('Quality Gate') {
      when { expression { env.EFFECTIVE_BRANCH in ['DEV','QA','PROD'] } }
      steps {
        timeout(time: 2, unit: 'MINUTES') {
          waitForQualityGate abortPipeline: false
        }
      }
    }

    stage('Package artifact') {
      when { expression { env.EFFECTIVE_BRANCH == 'PROD' } }
      steps {
        echo "📦 Archivando artefactos"
        archiveArtifacts artifacts: 'build/**', fingerprint: true, onlyIfSuccessful: true
      }
    }

    stage('Deploy to PROD') {
      when { expression { env.EFFECTIVE_BRANCH == 'PROD' } }
      steps {
        echo "🚀 Despliegue a PRODUCCIÓN (placeholder)"
      }
    }
  }

  post {
    success { echo "✅ Pipeline OK en ${env.EFFECTIVE_BRANCH}" }
    failure { echo "❌ Pipeline FAIL en ${env.EFFECTIVE_BRANCH}" }
    always  { echo "🏁 Fin de ejecución" }
  }
}
