pipeline {
  agent any

  options {
    timestamps()
    ansiColor('xterm')
    disableConcurrentBuilds()
    // Evita el checkout implícito duplicado (útil en multibranch)
    // skipDefaultCheckout(true)
  }

  environment {
    // CRA no trata warnings como errores si CI está vacío (solo durante build)
    CI = ''
    NODE_ENV = 'production'
    // Memoria de Node (opcional)
    NODE_OPTIONS = "--max_old_space_size=4096"
  }

  stages {

    stage('Checkout') {
      steps {
        checkout scm
        script {
          // Si NO es multibranch, BRANCH_NAME puede venir null → fallback DEV
          env.EFFECTIVE_BRANCH = (env.BRANCH_NAME ?: 'DEV')
          echo "📥 Rama detectada: ${env.EFFECTIVE_BRANCH}"
        }
      }
    }

    stage('Install dependencies') {
      steps {
        echo "📦 npm ci (o npm install)"
        bat 'npm ci || npm install'
      }
    }

    stage('Run tests (coverage)') {
      steps {
        echo "🧪 npm test -- --coverage --watchAll=false --ci --passWithNoTests"
        bat 'npm test -- --coverage --watchAll=false --ci --passWithNoTests'
      }
      post {
        unsuccessful { echo "❌ Fallaron las pruebas unitarias" }
        success      { echo "✅ Pruebas unitarias OK" }
      }
    }

    stage('Build app') {
      steps {
        echo "🏗  npm run build"
        bat '''
          set CI=
          npm run build
        '''
      }
    }

    // Análisis solo en DEV y QA (Community Edition no soporta branch analysis “nativo”)
    stage('SonarQube Analysis') {
      when {
        expression { return (env.EFFECTIVE_BRANCH in ['DEV', 'QA']) }
      }
      steps {
        echo "🔍 Enviando análisis a SonarQube…"
        withSonarQubeEnv('sonar-local') {
          withCredentials([string(credentialsId: 'sonarqube-token', variable: 'SONAR_TOKEN')]) {
            script {
              def scannerHome = tool 'sonar-scanner-win'
              // Un proyecto por rama (para Community Edition)
              def pKey  = "frontend-proyecto-final-${env.EFFECTIVE_BRANCH}"
              def pName = "frontend-proyecto-final (${env.EFFECTIVE_BRANCH})"

              bat """
                "${scannerHome}\\bin\\sonar-scanner.bat" ^
                  -Dsonar.projectKey=${pKey} ^
                  -Dsonar.projectName=${pName} ^
                  -Dsonar.sources=src ^
                  -Dsonar.sourceEncoding=UTF-8 ^
                  -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info ^
                  -Dsonar.login=%sqa_d88c9d69aaa3b92fdfa0cb421dc1a7663c167d31%
              """
            }
          }
        }
      }
    }

    // (Opcional) Esperar Quality Gate en Jenkins
    stage('Quality Gate') {
      when {
        expression { return (env.EFFECTIVE_BRANCH in ['DEV', 'QA']) }
      }
      steps {
        timeout(time: 2, unit: 'MINUTES') {
          // Si no quieres que falle el pipeline pon abortPipeline: false
          waitForQualityGate abortPipeline: false
        }
      }
    }

    // Solo PROD: empaquetado / despliegue
    stage('Package artifact') {
      when { expression { env.EFFECTIVE_BRANCH == 'PROD' } }
      steps {
        echo "📦 Archivando artefactos"
        archiveArtifacts artifacts: 'build/**', fingerprint: true, onlyIfSuccessful: true
      }
    }

    stage('Deploy to PROD') {
      when { expression { env.EFFECTIVE_BRANCH == 'PROD' } }
      steps {
        echo "🚀 Despliegue a PRODUCCIÓN (placeholder)"
      }
    }
  }

  post {
    success { echo "✅ Pipeline OK en rama ${env.EFFECTIVE_BRANCH}" }
    failure { echo "❌ Pipeline FAIL en rama ${env.EFFECTIVE_BRANCH}" }
    always  { echo "🏁 Fin de ejecución" }
  }
}
